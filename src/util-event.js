!
function (util) {
    util.event = {
        on: function () {
            
        }
        ,once: function () {
            
        }
        ,off: function () {
            
        }
        ,emit: function () {
            
        }
    };
    util.event.addListener = util.event.on;
    util.event.removeListener = util.event.off;
}
(simpleXDM._util);